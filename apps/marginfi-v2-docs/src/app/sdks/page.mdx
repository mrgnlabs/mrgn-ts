import { Libraries } from '~/components/Libraries'

export const metadata = {
  title: 'marginfi SDKs',
  description:
    'marginfi offers TypeScript and Rust SDKs to make your life easier and give you the best experience when interacting with the marginfi protocol.',
}

export const sections = [
  { title: 'Official libraries', id: 'official-libraries' },
]

# marginfi SDKs

The recommended way to interact with the marginfi protocol is by using one of our official SDKs. Today, Protocol offers fine-tuned TypeScript and Rust (CLI) libraries to make your life easier and give you the best experience when interacting with the marginfi protocol. {{ className: 'lead' }}

<Libraries />
