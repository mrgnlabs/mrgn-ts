import { Connection, clusterApiUrl } from "@solana/web3.js";

const SOLANA_NETWORK = "mainnet-beta";

// export const connection = new Connection(process.env.NEXT_PUBLIC_SOLANA_RPC_URL || clusterApiUrl(SOLANA_NETWORK), {
//     commitment: 'confirmed',
//   })
